#include "bcvm.h"
#include <vector>
#include <string>

using namespace std;

//returns the new index
void bc::bcvm::parseCmdLineArg(const char * p_args[], int p_i)
{
	int index = p_i;
	string arg(p_args[index]);
		
}

void bc::bcvm::run()
{
	//main loop
	
}